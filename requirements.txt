# VFX_Asset_Hub Requirements
# Python 2.7 compatible dependencies

# Note: For Python 3 development, use these versions instead:
# PySide2>=5.15.0
# Pillow>=8.0.0

# Python 2.7 versions (legacy VFX pipelines):
PySide2==5.13.2  # Last version supporting Python 2.7
Pillow==6.2.2    # Image processing for preview generation (last Py2.7 version)

# Python 3+ versions (recommended for development):
# PySide2>=5.15.0
# Pillow>=10.0.0
# IMPORTANT: NumPy must be < 2.0 for PySide2 compatibility
numpy<2.0.0  # PySide2/shiboken2 requires NumPy 1.x
